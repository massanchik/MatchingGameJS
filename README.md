# MatchingGameJS
Simple Match {N} game written in HTML + CSS(stylus) + JS(ES6)

![](matching-game-js.gif)

## Setup project
```shell
npm i -g parcel-bundler
npm i
```

## Run Dev Server
```shell
parcel index.html
```
